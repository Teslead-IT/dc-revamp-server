# Server Configuration
NODE_ENV=development
PORT=3011
BASE_URL=http://localhost:3011

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dc
DB_USER=postgres
DB_PASSWORD=1234

# Database Sync (set to 'true' to automatically sync schema on server start)
# WARNING: Set to 'false' in production and use migrations instead
DB_SYNC=false

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_here
JWT_REFRESH_SECRET=your_refresh_secret_key_here
JWT_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=http://localhost:3012
